version: 2

sources:
  - name: taxi_raw
    description: "Raw data landed by our ingestion scripts, untransformed."
    schema: taxi_raw
    tables:
      - name: yellow_tripdata
        description: "Yellow taxi trips as downloaded from NYC TLC, one file per month."